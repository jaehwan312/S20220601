<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.oracle.S20220601.profileMapper_jh">
	<select id="jhUserName" parameterType="int" resultType="String">
		select name from profile where mem_num=#{mem_num}
	</select>
</mapper>