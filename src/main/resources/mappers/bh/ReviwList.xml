<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oracle.S20220601.ReviewListMepper">
	
	<select id="reviewList" parameterType="int" resultType="Review">
	select * from review where host_num=#{host_num}
	
	</select>
	
	<select id="reviewPhotoList" parameterType="int" resultType="RevPhoto">
	select * from revphoto where host_num=#{host_num}
	
	</select>





</mapper>
  
  